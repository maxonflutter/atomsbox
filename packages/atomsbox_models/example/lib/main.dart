import 'package:flutter/material.dart';

void main() => runApp(const AtomsboxAnimationDemo());

class AtomsboxAnimationDemo extends StatelessWidget {
  const AtomsboxAnimationDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Simple',
    );
  }
}
